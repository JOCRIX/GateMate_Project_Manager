# Core dependencies for Cologne Chip Project Manager
pyyaml>=6.0.1

# GUI dependencies
PyQt5>=5.15.0

# Zector ZI-0001 serial programming
pyserial>=3.5

# Cross-platform terminal support (Windows only)
windows-curses>=2.3.1; sys_platform == "win32"

# Note: FPGA EDA tools (GHDL, Yosys, nextpnr-himbaechel, gmpack, Icarus
# iverilog/vvp, GTKWave, openFPGALoader) are external toolchain binaries,
# typically from OSS CAD Suite / Auto-Setup — not pip packages.